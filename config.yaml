# Central configuration for the SIH 3D drone pipeline.
input:
  video_path: data/input_video/input.mp4
  frames_dir: data/frames

depth_estimation:
  output_dir: data/depth_maps

dynamic_masking:
  output_dir: data/masked_frames

reconstruction:
  output_dir: data/output
